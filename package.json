{
  "name": "ecommerce",
  "version": "1.0.0",
  "main": "index.js",


  
  "scripts": {
   
    "test": "cypress run",
    "test:headed": "cypress run --headed",
    "test:browser": "cypress run --browser chrome",
    "open": "cypress open",
     "report": "mkdir -p reports && mochawesome-merge reports/*.json > reports/index.json && marge reports/index.json",
    "cy:run": "npx cypress run --browser chrome --spec 'cypress/e2e/**/*.cy.js'"


  },
  "author": "Dilip",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/cypress": "^0.1.6",
    "@types/mocha": "^10.0.10",
    "cypress": "^14.2.0",
    "cypress-xpath": "^2.0.1",
    "mocha-parallel-tests": "^2.3.0"
  },
  "dependencies": {
    "cypress-parallel": "^0.15.0"
  }
}
